program project1;
var
  N:array of integer;
  len_N, b, c: integer;
  strik,J: string;
  H:array [1..2] of integer;
begin
  Randomize;
  readln(len_N);
  SetLength(N,len_N);
  for b := 1 to len_N do
  begin
    N[b] := Random(100);
    write(N[b],' ');
  end;
  writeln;

  for b := 1 to len_N do
  begin
    J := IntToStr(N[b]);
    strik := strik + B; // Правильное добавление числа к строке
  end;
  writeln(strik);

  Readln();
end.

